{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile",
      "type": "shell",
      "command": "npm --prefix extension run compile",
      "problemMatcher": "$tsc",
      "group": "build"
    }
  ]
}
